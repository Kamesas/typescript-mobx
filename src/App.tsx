import * as React from "react";

export interface AppProps {}

const App: React.SFC<AppProps> = () => {
  return (
    <div className="app">
      <h1>App</h1>
    </div>
  );
};

export default App;
